# Docs

| Topic | Where |
|---|---|
| Architecture (containers, sequences, state machines, data model) | [`architecture/`](architecture/) |
| Testing strategy (TDD policy, how to run) | [`testing/`](testing/) |
| Operations (cron routines, GitHub sync, runbook) | [`operations/`](operations/) |
| Architecture Decision Records | [`adr/`](adr/) |
| Diagram sources + rendered SVGs | [`diagrams/`](diagrams/) |

The top-level [`README.md`](../README.md) is the entry point and the quickstart. This folder holds everything that's too deep for the README.
